<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <!-- Brand & toggle get grouped together for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand" href="/"> Job-listing7</a>
      </div>

    <!-- collect nav link,form for toggling  -->
    <div class="collapse navbar-collapse" id=bs-example-collapse-1>
      <ul class="nav navbar-nav pull-right">
        <% if !current_user %>
             <li> <%= link_to("Signup", new_user_registration_path) %></li>
             <li> <%= link_to("Signin", new_user_session_path) %> </li>
        <% else %>
            <li class="dropdown">
              <a href="#" class="data-toggle" data-toggle="dropdown">
                Hi!, <%= current_user.email %>
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to("Logout", destroy_user_session_path, method: :delete) %></li>
              </ul>
            </li>
          <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse end -->
  </div>
  <!-- /.container end -->
</nav>
